<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!--
        ACCESS_BACKGROUND_LOCATION — obbligatorio su Android 10+ per ricevere
        aggiornamenti GPS quando lo schermo è spento o l'app è in background.
        Senza questo il sistema throttola il GPS a ~una volta ogni 10 minuti.
        Deve essere richiesto SEPARATAMENTE da ACCESS_FINE_LOCATION a runtime:
        dopo aver ottenuto ACCESS_FINE_LOCATION, mostra una spiegazione e poi
        lancia requestPermissions con ACCESS_BACKGROUND_LOCATION da solo.
        Android porta l'utente direttamente alla schermata "Consenti sempre" nelle
        impostazioni di sistema — non si può fare inline come gli altri permessi.
    -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!--
        REQUEST_IGNORE_BATTERY_OPTIMIZATIONS — permette all'app di chiedere
        all'utente di essere esclusa dall'ottimizzazione batteria di Android.
        Necessario per mantenere la connessione GATT BLE (OBD) stabile anche
        quando lo schermo è acceso ma il sistema è sotto pressione di memoria.
    -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="Duke 390"
        android:theme="@style/Theme.Duke390Dash"
        android:supportsRtl="true">

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:keepScreenOn="true"
            android:screenOrientation="portrait"
            android:launchMode="singleInstance"
            android:windowSoftInputMode="stateHidden|adjustNothing"
            android:theme="@style/Theme.Duke390Dash.NoAnimation"
            android:excludeFromRecents="true"
            android:resizeableActivity="false">
            <!--
                HOME intent filter sulla MainActivity (non SplashActivity) —
                così Android sa che è questa la home vera e gestisce correttamente
                i gesti di navigazione senza aprire il task switcher sopra di essa.
            -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="android.notch_support"
                android:value="true"/>
        </activity>

        <activity
            android:name=".AnalyzerActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden"
            android:theme="@style/Theme.Duke390Dash.NoAnimation"/>

        <!--
            foregroundServiceType="connectedDevice|location":
            - connectedDevice: BLE / OBD (era già presente)
            - location: necessario per usare LocationManager in un ForegroundService
              su Android 10+. Senza questo il GPS nel service viene ignorato/throttolato.
        -->
        <service
            android:name=".DashForegroundService"
            android:foregroundServiceType="connectedDevice|location"
            android:exported="false" />

        <service
            android:name=".DashTileService"
            android:label="Duke 390"
            android:icon="@mipmap/ic_launcher"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true"/>
        </service>

    </application>

</manifest>
